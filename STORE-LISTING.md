# Chrome ウェブストア 掲載情報（提出用の下書き）

そのままデベロッパーダッシュボードに貼り付けられる形にしてあります。
`（要記入）` の箇所だけ埋めてください。

---

## 拡張機能名（75文字以内）

```
Web Page Summarizer - Local AI
```

## 概要 / Summary（132文字以内）

```
表示中のページを、Chrome内蔵AIが端末内で要約。ページの内容は外部に一切送信されません。APIキー不要、無料。
```

現在 56文字。

## 詳細な説明

```
表示中のWebページの本文を、サイドパネルに要約して表示します。

■ 内容が外部に出ません
要約はChromeに組み込まれたAI（Gemini Nano）が、あなたのパソコンの中だけで
生成します。ページの内容がサーバーへ送信されることは一切ありません。
社内資料や下書き中のページでも安心して使えます。

■ APIキーも登録も不要
OpenAIやGeminiのAPIキーを用意する必要はありません。アカウント登録も課金も
ありません。インストールしてすぐ使えます。

■ 使い方
1. 要約したいページでツールバーのアイコンをクリック
2. 右側にサイドパネルが開き、自動で要約が表示されます

■ 設定でカスタマイズ
・要約の言語：英語 / 日本語 / 中国語（簡体字）
・要約の長さ：短い / ふつう / 長い
・要約の形式：箇条書き / TL;DR / ティザー / 見出し
・パネルを開いたときの自動要約のオン・オフ

■ 動作要件
・Chrome 138以降（パソコン版）
・初回利用時に、Chromeが約2GBのAIモデルをダウンロードします
・空きストレージ22GB以上、VRAM 4GB以上

■ 注意事項
・chrome:// で始まるページやChromeウェブストア上では動作しません
・PDFファイルの要約には対応していません
・オフラインでも動作します（初回のモデルダウンロード後）
```

## カテゴリ

生産性向上（Productivity）

## 言語

日本語（必要に応じて英語も追加）

---

## プライバシー関連の申告

### 単一用途の説明

```
表示中のWebページの本文を要約し、サイドパネルに表示することが唯一の目的です。
要約はChrome内蔵のAI（Summarizer API / Gemini Nano）により端末内で生成され、
ページの内容は外部に送信されません。
```

### 各権限の使用理由

**activeTab**
```
ユーザーがツールバーのアイコンをクリックしたタブに限り、要約対象となる本文
テキストを読み取るために使用します。
```

**scripting**
```
本文テキストを抽出するスクリプトを対象タブで実行するために使用します。抽出
した内容はメモリ上でのみ処理し、保存も送信も行いません。
```

**sidePanel**
```
生成した要約を表示するためのサイドパネルを開くために使用します。
```

**storage**
```
要約の言語・長さ・形式、および自動要約のオン／オフというユーザー設定を保存
するために使用します。閲覧内容や個人情報は保存しません。
```

**ホスト権限 `<all_urls>`（オプション権限）**
```
既定では要求しません。ユーザーが設定画面で明示的に有効にした場合にのみ、Chrome
の権限ダイアログを通じて許可を求めます。タブを切り替えた後もアイコンを押し直さ
ずに要約できるようにするためのものです。設定を無効にすると権限も取り消されます。
```

### リモートコードの使用

```
使用していません。すべてのコードは拡張機能のパッケージに含まれています。
```

### データ使用に関する申告

すべての項目で「収集しない」を選択し、以下3つの証明にチェックします。

- 認証されている用途以外でデータを使用しません
- 承認されている用途以外でデータを販売・譲渡しません
- 信用調査や融資目的でデータを使用・転送しません

### プライバシーポリシーのURL

```
（要記入）
```

`PRIVACY.md` の内容を公開ページとして設置し、そのURLを記入してください。

---

## 必要な画像素材

| 種類 | サイズ | 必須 | 内容 |
| --- | --- | --- | --- |
| アイコン | 128×128 | 必須 | `icons/icon128.png`（作成済み） |
| スクリーンショット | 1280×800 | 最低1枚 | 要約が表示されたサイドパネル |
| 小さいタイル | 440×280 | 任意 | 掲載品質が上がるため推奨 |

### スクリーンショットの撮り方

1. 適当な記事ページで拡張機能を起動し、要約を表示させる
2. ウィンドウ全体を 1280×800 で撮影（Windowsは `Win + Shift + S`）
3. サイドパネルと元ページの両方が写る構図にすると、用途が一目で伝わります

3〜5枚あると訴求力が上がります。おすすめの構成は次のとおりです。

- 要約が表示された状態（メイン）
- 設定画面
- 「端末内で処理」を訴求するテキスト入りの説明画像

---

## 提出前チェックリスト

- [ ] 実機で動作確認（アイコンクリック → ダイアログなしで要約が表示される）
- [ ] `manifest.json` のバージョンを確認
- [ ] zip を作成（`git archive` でコミット済みファイルのみ）
- [ ] プライバシーポリシーを公開してURLを取得
- [ ] スクリーンショットを1枚以上用意
- [ ] 公開範囲を「限定公開」に設定して提出
